/* 
[문제]
*중요* 외우는것이 아니라 필요하면 그때 찾아보는 것이다. 

2. 다음에서 설명하고 있는 함수는 각각 무엇인지 적절한 함수 이름을 고르시오.

1) strlwr() = 문자열 str을 모두 소문자로 변환하고 변환한 문자열을 반환한다.
2) strlen() = 문자열 str에서 NULL 문자를 제외한 문자열의 길이를 반환한다.
*3) strpbrk()  = 앞의 문자열 str에서 뒤 문자열 charset에 포함한 문자가 나타나는 처음 위치를 찾아
그 주소값을 반환한다.
*4) strstr() = 앞의 문자열 str에서 뒤 문자열 strsearch가 나타나는 처음 위치를 찾아 그 주소값을 반환한다.
5) strchr() = 앞의 문자열 str에서 뒤 문자 ch가 나타나는 처음 위치를 찾아 그 주소값을 반환한다.
6) memcpy() = 포인터 src 위치에서 dest에 n바이트를 복사한 후 dest위치 반환한다.
*/
