/* 
[문제]
1. 다음에서 빈 부분을 적절히 채우시오.

1) 문자를 저장하려면 "char"형 변수를 사용한다.
2) 함수 gets(), puts(), get_s()를 사용하려면 헤더파일 "stdio.h"를 삽입해야 한다.
3) 함수 "strcpy()"는 앞 문자열 dest에 처음에 뒤 문자열 null 문자를 포함한 source를 복사하여
그 복사된 문자열을 반환하는 함수이다.
4) 함수 gets()는 표준입력으로 [Enter] 키를 누를 때까지 공백을 포함한 한 행의 모든 문자열을 입력받는다.
입력된 문자열에서 마지막 [Enter] 키를 "\0" 문자로 대체하여 저장한다.
5) 함수 "strstr()" 은 앞 인자 문자열에서 뒤 인자 문자열이 나타나는 처음 위치를 찾아 그 주소값을 반환한다.
만약 찾기 못하면 NULL 포인터를 반환한다. 
*/
