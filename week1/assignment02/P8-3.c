/*  Pratice 08 - [문제]
3. 다음에서 서술된 내용이 맞으면 ○, 틀리면 ×를 표시 하시오.
(보고서에 문제에 대한 풀이를 함께 간단히 서술하시오.)
질문 - 5번

1. 변수의 주소는 ‘%d’로 출력할 수 있다. ( O ) %u 또는 %d로 직접출력한다. 
2. double형 포인터 pd의 값이 100이라면, (pd+1)은 101이며, (pd-1)은 99가 된다.   ( x ) (pd+1) 은 주소값 +1 이므로 108가 된다.
3. 포인터 변수는 일반 변수 선언과 같다. ( x ) 포인터변수는 주소값을 저장하는 변수로 연산자 *를 쓴다.
4. 주소 연산자 &는 ‘&32’와 ‘&(3+4)’와 같이 상수나 표현식에는 사용할 수 없다.    ( O ) &연산자는 전위연산자로 '변수'에만 사용가능하다.
5. 메모리 주소는 0부터 워드마다 1씩 증가한다    ( X ) 포인터변수는 종류에 관계없이 크기가 모두 4바이트이므로 4씩 증가한다.
*/
